---
trigger: always_on
---

## Next.js App Router
- When creating a new page route (`page.tsx`), ALWAYS implement a corresponding `loading.tsx` file.
- Use the `Skeleton` component from `@/components/ui/skeleton` for loading states.
- Ensure loading states mimic the layout of the page they are loading for.
